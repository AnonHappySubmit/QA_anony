from QA_tool import *

# fold_root = '**'
# save_dicom = '**'

# save_length = '**'

# #save_distance = '**'

# #instanceN_fold_NLST(fold_root, save_dicom)

# #sliceDis_fold_NLST(fold_root, save_distance)

# #filter_few_slices(save_dicom)

# lenbody_fold_NLST(fold_root, save_length)

# check_affine_fold_NLST(fold_root = '**',
#                   save_csv_path = '**')

#check_affine_fold_NLST(fold_root = '**',
#                  save_csv_path = '**')

# max_res_fold_NLST(fold_root = '**',
#                   save_csv_path = '**')

# max_res_fold_NLST(fold_root = '**',
#                   save_csv_path = '**')

# ------------------ The follow for project ----------------------------- # 

# fold_root = '**'

# save_csv_path = '**'

# lenbody_fold(fold_root, save_csv_path)

lenbody_fold(fold_root = '**', save_csv_path = '**')

# max_res_fold_NLST(fold_root = '**',
#                   save_csv_path = '**')

# check_affine_fold_NLST(fold_root = '**',
#                   save_csv_path = '**')
